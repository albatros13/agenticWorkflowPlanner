* (+) BPMN generation from natural language process descriptions - implement and test
* (+) App should show visual representation of the BPMN process
* (+) App should support editing and saving the BPMN process
* (+) Represent decisions with Decison Model Notation (DMN) for BPMN
* (+) Generate tests for the model

* Executable workflow generation from natural language process descriptions - investigate formats
